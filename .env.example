PORT=3000

RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=5

# Set to true when running behind a reverse proxy so Express reads
# X-Forwarded-* correctly. You can also set it to a hop count ("1")
# or a subnet string accepted by Express.
TRUST_PROXY=false

SHUTDOWN_TIMEOUT_MS=10000
